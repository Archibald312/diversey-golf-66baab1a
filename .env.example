# Environment Variables Reference
# Copy this file to .env.local and populate with your actual secrets
# IMPORTANT: Never commit .env.local or any file containing real secrets to Git

# ============================================
# Security & Authentication
# ============================================

# Vercel Blob Storage: Read/Write token for private blob access
# Get this from: Vercel Dashboard > Storage > Blob > Tokens
BLOB_READ_WRITE_TOKEN=your_blob_read_write_token_here

# Export Waitlist Secret: Used to secure the /api/export-waitlist endpoint
# Must be a strong random string; sent as Authorization: Bearer {EXPORT_SECRET}
EXPORT_SECRET=your_strong_random_secret_here

# ============================================
# CORS & Origin Configuration
# ============================================

# Allowed Origins: Comma-separated list of trusted origins for CORS
# Example: "https://yourdomain.com,https://app.yourdomain.com,http://localhost:5173"
ALLOWED_ORIGINS=http://localhost:5173,https://yourdomain.com

# Alternative (if using single frontend origin):
# FRONTEND_ORIGIN=https://yourdomain.com

# ============================================
# Notes
# ============================================
# - All tokens and secrets should be rotated regularly (every 90 days recommended)
# - Use strong, randomly-generated secrets for EXPORT_SECRET (minimum 32 characters)
# - Store production secrets in Vercel Environment Variables, never in source control
# - For local development, create a .env.local file (already in .gitignore) with test values
# - Enable GitHub secret scanning to prevent accidental commits of secrets
